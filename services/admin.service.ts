// import { api } from "@/lib/axios";
// import { ADMIN } from "@/lib/endpoints";

// export const adminService = {
//   login: (data: { email: string; password: string }) =>
//     api.post(ADMIN.LOGIN, data),

//   resetPassword: (data: {
//     email: string;
//     token: string;
//     password: string;
//   }) => api.post(ADMIN.RESET_PASSWORD, data),
// };
